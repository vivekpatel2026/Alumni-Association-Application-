import { createAction} from "@reduxjs/toolkit";

//! Reset Success action
export const resetSuccessAction = createAction("reset-success-action");


//! Reset error action

export const resetErrorAction = createAction("reset-error-action");
